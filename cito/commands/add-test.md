---
description: Grunt task — add a test for one file in the repo's existing style.
argument-hint: [file or function to test]
---

## task

- Read `${CLAUDE_PLUGIN_ROOT}/agents/cito-add-test.md` in full.
- Follow it exactly in this session.
- Do not spawn a subagent.

## input

- target: `$ARGUMENTS`

## if missing

- If no target is given above, use the one from the conversation so far.
- If that is still unclear, ask.
